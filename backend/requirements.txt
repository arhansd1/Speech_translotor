# Core
fastapi==0.115.5
uvicorn[standard]==0.32.1
python-multipart==0.0.12   # Required for File/Form uploads in FastAPI

# HTTP client
httpx==0.27.2

# LangGraph + LangSmith
langgraph==0.2.55
langchain-core==0.3.25
langsmith==0.1.147          # Automatic tracing when LANGCHAIN_API_KEY is set

# Eval scoring
sacrebleu==2.4.3

# Environment
python-dotenv

# Async utilities (used for sleep/gather in evals)
# asyncio is stdlib — no install needed
